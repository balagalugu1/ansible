---
plugin: aws_ec2

regions:
  - us-east-1

keyed_groups:
  - key: tags
    prefix: tag

  - prefix: instance_type
    key: instance_type

  - key: placement.region
    prefix: aws_region

host_vars:
  ansible_user: ubuntu
  ansible_ssh_private_key_file: /root/ansibleadminkey.pem
